import MainPage from '../pageobjects/main.page'

describe('Button Details is existing in #imageItem14', () => {
    beforeEach(() => {
        MainPage.open();
    });

    it('Existing ', () => {
    });
});