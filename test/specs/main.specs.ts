import LoginPage from '../pageobjects/login.page';
import MainPage from '../pageobjects/main.page'

describe('main page', () => {
    beforeEach(() => {
        MainPage.open();
    });

    it('Existing ', () => {
    });
});